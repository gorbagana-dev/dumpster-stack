services:
  dumpster-maintenance:
    image: caddy:alpine
    ports:
      - "8000"
    volumes:
      - maintenance-html:/srv
    command: caddy file-server --root /srv --listen :8000

volumes:
  maintenance-html:
